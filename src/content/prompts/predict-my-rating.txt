  First, call `get_tool_guide` to read the workflow guidance for this server, and read info://1001-albums/about to understand how the
  generator assigns albums and what ratings represent.

  Using the 1001 Albums tools, I want you to predict how I will rate today's album for project "${projectIdentifier}" and explain your reasoning.

  When interpreting decade averages, note that the musical era of an album often differs
  from its recording date. Assess the album's actual musical character rather than its
  release year label.

  To do this well, you MUST follow this sequence:
  1. **Identify the album**: Fetch today's album using `get_album_of_the_day` to know what it is (name, UUID, genres, styles, release year). Note the album's UUID from this response. Use it — not the album name — as the `albumIdentifier` in all subsequent tool calls in this workflow.
  2. **Establish taste character**: Call `get_rating_outliers` BEFORE forming any hypothesis. Read the "overrated by user" list carefully to identify the recurring stylistic patterns (energy, rawness, era, format) that reveal my genuine enthusiasms. This is your most reliable predictor.
  3. **Get qualitative evidence**: Use `get_review_insights` with `albumIdentifier` set to today's album name or UUID. Do NOT use the `query` parameter. This synthesises reasoning from stylistically connected albums in my history.
  4. **Get musical context & follow up**: Use `get_album_context` to see how this album fits musically. For any closely connected album (especially same artist, same style, or same era/format), call `get_album_detail` to read my actual review.
  5. **Character search**: Use your understanding of the album's actual character — not just its genre label — to run one or two targeted `search_project_history` queries for albums that share the same experiential qualities. Think about what makes this album distinctive: its energy, mood, instrumentation, cultural origin, format, or the era it actually sounds like rather than when it was recorded. The 1001 Albums list specifically includes records that were ahead of their time or defined a moment — genre taxonomy often undersells what makes them similar to other things in someone's history. Use the results to surface any character-specific patterns that `get_album_context`'s genre matching may have missed.
  6. **Contextualise with statistics**: Call `get_taste_profile`. Use decade and genre averages as secondary, contextualising signals only — do not use them as the primary foundation for your prediction.
  7. **Arc check (Optional)**: If I have a long history, use `get_listening_arc` to see if my taste has drifted recently.

  **Signal Weighting**:
  When signals conflict, weight them in this order:
  1. **Rating outlier patterns** (genuine enthusiasms/aversions)
  2. **Written reviews** (via `get_review_insights` and `get_album_detail`)
  3. **Genre/style affinities**
  4. **Decade averages** (treat with caution)

  **Output**:
  Give me a specific predicted rating (e.g. 3.5/5) with a confidence level (1-5) and a detailed explanation of your reasoning. Reference my actual past ratings and reviews as evidence. Identify the single strongest signal that drove your prediction. Flag any uncertainties.
